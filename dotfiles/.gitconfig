[core]
	editor = vim
    pager = "diff-so-fancy | less --tabs=4 -RFX" 
[user]
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[alias]
    st = status
    di = diff --ignore-all-space
    ci = commit
    cim = commit -m
    co = checkout
    br = branch
    l1 = log --graph -n 50 --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset)%C(dim white) %an%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(bold yellow)%d%C(reset)' --all
    l2 = log         -n 50 --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim red)%an%C(reset)%C(bold yellow)%d%C(reset)' --all
	d = difftool
[color]
	ui = true
[merge]
	tool = vimdiff
	conflictstyle = diff3
[mergetool]
	prompt = false
